export type Test = {
  test: string;
};

// 레시피 카드 타입
export type recipeType = {
  recipeTitle: string;
  recipeImg: string;
  viewCnt: number;
  likeCnt: number;
  commentCnt: number;
  userName: string;
  userImg: string;
  createdDate: string;
  recipeId: number;
};
